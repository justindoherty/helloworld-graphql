# Root Query
type Query {
    # The hello
    hello: String
    human: Person
}

type Person {
    name: String!
    age: Int
    dog: Dog
}

type Dog {
    name: String!
}

type Subscription {
    thing: String
}

schema {
  query: Query
  subscription: Subscription
}